# Better-money 个人智能记账与储蓄助手 — 设计文档

> 目标用户：想攒钱但攒不住钱的学生。核心诉求：记账省事、随时知道还剩多少钱、
> 买东西有目标感、每周/每月有人情味的总结、看图看占比。

---

## 1. 核心闭环

```
钱从哪来（收入/起始余额）→ 钱花到哪（每日记账）→ 还剩多少（实时余额）
→ 目标进度（想买的东西）→ 周/月复盘（小作文+图表）→ 下阶段调整
```

## 2. 必须确定的业务规则（待拍板）

| 决策点 | 选项 | **已确认** |
|---|---|---|
| 收入模型 | 按月固定生活费 / 不定期（兼职、红包）/ 混合 | **不定期收入**：随时记一笔「兼职 200」，按收入自动预留一部分进目标 |
| 账户模型 | 单账户合计 / 微信、支付宝、银行卡、现金分账户 | 单账户合计，减少录入负担 |
| 预算模型 | 月总额 / 每日限额 / 分类预算 | 月总额为主，自动拆出"今日可花额度" |
| 目标模型 | 单个目标 / 愿望清单（多目标+优先级） | **愿望清单**：多目标、可排序、暂停、放弃 |
| 使用场景 | 手机 / 电脑 / 机器人 | **纯电脑本地使用**：每晚输入当天花销，可补记前几天的账 |
| 部署 | 本地 / 云服务器 | **本地电脑跑**：一个命令启动，数据全在自己手里 |
| 分类体系 | 固定分类 / AI 自动分类 / 自定义 | 固定分类打底 + AI 自动归 + 可改名合并 |
| 数据修正 | 记错怎么改/删 | 每条记录可改可删，AI 可"撤销上一笔" |
| 对账机制 | 账本余额 vs 真实余额 | 支持一键校准：输入真实余额，差额自动记为"差额调整" |

### 2.1 初始化（第一次用必须做）
1. 输入当前总余额（微信+支付宝+银行卡+现金加总）。
2. 设定月预算（比如生活费 2000，其中想存 400）。收入不定期时，可设「每笔收入自动存 X%」规则。
3. 创建愿望清单（想买什么、价格、链接/截图、期望时间）。
4. 之后：`当前余额 = 初始余额 + 收入 − 支出 ± 差额调整`。

## 3. 核心功能（必须）

1. **多形态记账**：
   - 文字：「午饭 15」「打印资料 8 块」→ LLM 解析出 {金额, 分类, 商家, 时间, 备注}
   - 截图：外卖/支付截图 → 视觉模型识别金额、商家、时间
   - 银行流水：导入微信/支付宝/银行导出的 CSV 账单 → 程序直接解析（比截图可靠）
2. **实时余额**：任何时候问「我还剩多少钱」。
3. **目标追踪**：进度条、已存金额、按近期结余速度估算「预计 X 天后达成」。
4. **预算预警**：超 80% 提醒、超 100% 告警，AI 给出应对建议。
5. **周总结/月总结小作文**：见第 6 节。
6. **图表看板**：见第 5 节。

## 4. 建议新增的功能

### 4.1 对"攒不住钱"最有效的（强烈建议）
1. **今日可花额度**：「今天还剩 X 元可以花」——由月预算和已花金额动态计算，
   每天早上推送。这是防超支最直接的一招。
2. **冲动消费冷静期**：想买的东西先放进愿望清单，设冷静期（默认 7 天）。
   到期 AI 提醒「还想要吗？」；回答"不想要了"就省下一笔钱，并计入"省下的钱"统计。
3. **固定支出雷达**：会员订阅、话费、水电这类"不知不觉漏钱"的项目，自动识别
   并提醒（如「B 站大会员下月 8 日自动续费」）。
4. **储蓄率指标**：`月结余 ÷ 月收入`，目标可设（如 20%），这是攒钱的北极星指标。
5. **环比对比**：本月 vs 上月，哪类花多了、为什么，写进月总结。

### 4.2 体验加分（可选）
6. **打卡与徽章**：连续记账天数、连续 N 天不超预算、完成周目标等，轻微游戏化。
7. **总结配图**：生图 API 的合理用途——每周生成一张主题插画/海报（如"本周省钱之星"），
   让总结更有仪式感。⚠️ 统计图表不要用生图 API，见第 5 节。
8. **账单截止提醒**：缴费单截图里识别出截止日期，提前提醒。
9. **数据备份导出**：CSV/JSON 一键导出，防止数据丢失。
10. **语音记账**（可选）：说一句「买奶茶花了 12」自动入账。
11. **监督模式**（可选）：把周总结分享给家人/好友互相督促（注意隐私）。

## 5. 图表方案（纠正一个误区）

**统计图表不需要 GPT 生图 API**。生图模型画图表数字会错、成本高、还慢。
正确做法：

| 图表 | 类型 | 工具 |
|---|---|---|
| 花销占比 | 环形/饼图 | 前端 ECharts / Chart.js，或用 QuickChart 生成 PNG 推送 |
| 每日支出趋势 | 折线图 | 同上 |
| 周对比 | 柱状图 | 同上 |
| 目标进度 | 进度环 | 同上 |
| 余额曲线 | 面积图 | 同上 |

- 网页看板：ECharts 直接渲染（免费、准确、可交互）。
- 聊天里看图：QuickChart 按数据生成 PNG 图片发给用户。
- **生图 API（gpt-image-1 / DALL·E）只用于**：周/月总结的创意配图、海报、头像等。

## 6. 周/月总结"小作文"的设计（重点）

不做固定模板，关键是给 LLM 喂"素材"而不是"框架"：

**喂给模型的数据**：
- 本周/本月全部流水（金额、分类、商家、时间、备注）
- 预算执行情况（超支/结余、哪些天超标）
- 目标进度变化（这周向目标前进了多少）
- 与上一周期的对比（多花/少花在哪类）
- 特殊事件（大额消费、退款、兼职收入、冷静期放弃的购买）

**提示词要点**：
- 角色：一个懂你、会调侃也会鼓励的记账搭子，不是银行客服
- 要求：每次换一种开头和结构；必须引用具体事件（「周三那杯奶茶」而非「部分饮品消费」）；
  先肯定做得好的，再点出 1–2 个最值得改的；结尾给下周一条具体可执行的建议
- 长度：周总结 200–400 字，月总结 400–800 字
- 风格旋钮：可设「毒舌 / 温柔 / 老师 / 朋友」等语气档位

## 7. 怎么用才方便（交互设计，按"电脑+每晚记账"场景重写）

### 7.1 核心使用流程：每晚一次，5 分钟搞定
打开网页 → 页面默认就是"今天的账" → 在一个大输入框里把今天的花销一口气打进去：

```
午饭食堂 15
奶茶 12
打印资料 5
```

点「记一笔」→ AI 一次解析成 3 条记录，秒回确认，右侧看板余额、今日额度、
目标进度**立刻刷新**。这就是全部日常操作。

### 7.2 三种录入方式，按场景选
1. **文字批量**（主力）：多行输入，一条消息记多笔；支持自然语言
   「大概 30 块钱的饭」AI 会估算并标注，事后可改。
2. **截图识别**：拖入微信/支付宝账单截图，视觉模型自动提取金额、商家、时间，
   一次可识别一张图里的多笔。适合当天没记、第二天对着账单截图补。
3. **CSV 批量导入**：微信、支付宝都支持导出月度账单 CSV/XLSX，月底一次导入全月，
   最可靠、零遗漏，适合"几天没记账"的救场。

### 7.3 补记设计（你明确提到的需求）
- 输入框旁有**日期选择器**，默认今天；忘了记，第二天把日期拨回昨天即可。
- 看板按天展示，一眼看出**哪几天是空白的**，提醒你补记。
- 截图识别的记录自动带上截图里的时间，不统一记成今天。

### 7.4 看板就是首页，打开就有反馈
- 顶部：当前余额、本月已花/预算、今日可花额度、目标进度环
- 中部：分类占比饼图、每日支出趋势、周对比柱状图
- 底部：最近的周/月总结小作文（可手动点「生成本周总结」）
- 收入随时记：「兼职 200」走同一个输入框，AI 自动识别为收入并执行
  「自动存 X% 进目标」规则

### 7.5 桌面端的贴心细节
- 启动脚本：双击 `启动.bat` 自动开服务、自动打开浏览器
- 键盘友好：输入框回车即提交，Ctrl+Enter 换行
- 一键对账：卡里钱和账本对不上时，输入真实余额自动差额调整
- 本地数据随时导出 CSV/JSON 备份

## 8. 技术架构建议

```
[输入]  网页：文字批量输入 · 截图拖拽上传 · CSV 账单导入
     ↓
[后端]  Python FastAPI（一个服务，本地 127.0.0.1 运行）
     ↓
[AI 层]   文本解析/总结 → gpt-4o-mini（极便宜）
          截图识别 → gpt-4o 视觉（或国内 Qwen-VL）
          创意配图 → gpt-image-1（可选，用得很少）
          （做成可切换适配器，随时换模型）
     ↓
[存储]  SQLite 单文件（个人项目最合适，易备份）
     ↓
[输出]  本地网页看板（ECharts 图表）· 周/月总结小作文 · 目标进度
```

- 技术栈选定：**Python + FastAPI + SQLite + 原生 HTML/JS 前端（ECharts）**
  —— AI 生态最好、教程最多、单机一个命令启动、数据就是一个文件。
- 启动方式：双击 `启动.bat` → 自动启动服务并打开浏览器。

### 成本预估（按学生体量，月约 100 笔文字+30 张截图+4 周总结+1 月总结）
- gpt-4o-mini 文本解析与总结：**每月约 1–3 元人民币**
- gpt-4o 截图识别：每月约 2–5 元
- 图表：0 元（本地渲染）
- 合计：**每月 5 元以内**，足够个人使用

### 国内网络提示
- OpenAI API 在国内需特殊网络环境或中转服务；不想折腾可换：
  文本用 DeepSeek/Qwen（国内直连、更便宜），截图识别用 Qwen-VL（阿里云百炼）。
  架构上把 AI 层做成可切换的适配器，随时换模型。

## 9. 隐私提醒

截图和流水会发送给大模型厂商。个人自用风险可控，但建议：
- 导入账单时只保留交易行，剔除卡号等敏感列
- 不在备注里写密码、身份证号等
- 若介意，可把 AI 层换成本地部署的小模型（后续可扩展）

## 10. 已确认决策与实施计划

### 10.1 已拍板
1. ✅ 使用场景：**纯电脑本地使用**，每晚记账，支持补记，与手机无关
2. ✅ 技术栈：**Python + FastAPI + SQLite + 网页前端**（用户对语言无要求，选结果最好的方案）
3. ✅ 收入模式：**不定期收入**（兼职/红包/家里给），支持「每笔收入自动存 X%」
4. ✅ 目标：**愿望清单，多目标带优先级**
5. ✅ 部署：**本地电脑跑**，双击启动
6. ✅ 分类：8 类支出（奶茶咖啡单列）+ 4 类收入
7. ✅ AA 规则：用户补充说明（「4人AA」/「AA后我付了50」），AI 算实付金额，原总额存备注
8. ✅ 多目标资金分配：自动存全部给优先级第一（①A），可手动调拨
9. ✅ 买下目标：自动记一笔支出 + 目标标记已达成（②A）
10. ✅ AI 失败：界面红色横幅明说，用户自行解决；手动表单兜底，记账不中断
11. ⏳ AI 供应商（M2 开工前确认用哪家；适配器架构，不阻塞）：
    OpenAI（需中转）/ DeepSeek 文本 + Qwen-VL 视觉（国内直连）
12. ⏳ 初始化参数：月预算金额、收入自动存比例（默认 30%）、总结语气档位（默认朋友）
    → 首次使用填写，随时可改

### 10.2 实施里程碑（已全部完成 ✅）
| 阶段 | 内容 | 状态 |
|---|---|---|
| M1 骨架 | FastAPI 服务 + SQLite 表结构 + 启动脚本 | ✅ |
| M2 记账 | 文字批量解析（LLM）、手动表单、补记日期、收入 | ✅ |
| M3 补录 | 截图/小票识别（确认面板）、CSV 账单导入 | ✅ |
| M4 看板 | 余额/今日额度/目标进度 + ECharts 饼图、趋势、对比 | ✅ |
| M5 总结 | 周/月小作文生成（非模板化）+ 可选配图 | ✅ |
| M6 增强 | 预算预警、冷静期、储蓄率、目标清单、对账 | ✅ |
| M7 打磨 | 自动备份、数据导出、历史明细、使用说明 | ✅ |

---

## 11. 设计细节提案（逐条待确认）

### 11.1 界面布局（单页两栏，桌面大屏）

```
┌──────────────────────────────────────────────────────────────┐
│ 🐷 Better-money            [对账] [导出] [设置]                 │
├──────────────┬───────────────────────────────────────────────┤
│ 📝 记账       │ 💰 总览                                          │
│ 日期 [今天▾]  │  余额 ¥1234.5 · 本月 ¥800/¥1500 · 今日可花 ¥23   │
│ ┌──────────┐ │  🎯 目标：耳机 61% ▮▮▮▯▯ （预计还有 12 天）       │
│ │ 午饭 15   │ │  ┌──────────────┐ ┌──────────────┐            │
│ │ 奶茶 12   │ │  │ 本月分类占比   │ │ 近30天支出趋势 │            │
│ │ 打印 5    │ │  │   (饼图)      │ │   (折线)      │            │
│ └──────────┘ │  └──────────────┘ └──────────────┘            │
│ [✔ 记一笔]   │  📝 最近的总结：上周小作文（点击展开全文）          │
│ [📷 截图] [📄 导入CSV] [💰 记收入]                              │
│ ─ 今日明细 ─ │  [生成周总结] [生成月总结]                        │
│ ✔ 午饭 15    │                                                 │
│ ✔ 奶茶 12    │                                                 │
├──────────────┴───────────────────────────────────────────────┤
│ 顶部标签：看板 | 目标清单 | 总结 | 历史明细 | 设置                │
└──────────────────────────────────────────────────────────────┘
```

- 左侧固定记账区（最常用的操作永远在视线内），右侧看板滚动。
- 「目标清单」「总结」「历史明细」用顶部标签切换，不挤在首页。

### 11.2 默认分类（可改名、合并、增删）

| 支出分类 | 涵盖 |
|---|---|
| 🍜 餐饮 | 食堂、外卖、零食 |
| 🧋 奶茶咖啡 | **单列（已确认）**：奶茶、咖啡、饮品店——"可砍支出"重灾区，单列后总结可精准点评 |
| 🚌 交通 | 公交、地铁、打车 |
| 📚 学习 | 书本、打印、网课、考试报名 |
| 🛍 购物 | 衣物、日用品、数码 |
| 🎮 娱乐 | 游戏、电影、会员、出游 |
| 🏠 生活 | 话费、水电、理发、医疗 |
| 💸 其他 | AI 归不进时的兜底，可事后改 |

收入分类：兼职 / 红包 / 家里给 / 其他收入。
> 已确认：奶茶咖啡单列一类。共 8 类支出 + 4 类收入。

### 11.3 记账解析规则（AI 行为约定）

- **多行批量**：每行一笔；一行内多笔靠语义拆分（「食堂15和奶茶12」）。
- **模糊金额**：「大概30」→ 记 30 并标「估算」，随时可改。
- **完全没金额**：「买了瓶水」→ 不瞎猜，输入框下方反问「那瓶水多少钱？」，补答后入账。
- **收入自动识别**：「兼职200」「收到红包66」「家里给了500」→ 记入收入分类，
  并按规则自动分配 X% 到目标。
- **退款**：「退了奶茶12」→ 记为支出冲减，饼图里该分类金额对应减少。
- **时间**：默认当天；「昨天午饭15」自动记到昨天；截图按截图内时间。
- **AA 注释（已确认）**：截图/小票显示总额但不是你一个人承担时，在同一输入框补一句即可：
  「4个人AA」→ AI 自动算 200÷4=50 入账；「AA后我付了50」→ 直接记 50；
  原始总额保留在备注里。文字输入同理：「聚餐200 4人AA」。你请客则不注释，记全额。
- **防重复**：日期+金额+商家完全相同的记录提交时提示「可能重复」。

### 11.4 预算与储蓄算法

- **今日可花额度** =（月预算 − 本月已花）÷ 本月剩余天数。
  每天刷新；超支后显示为负数并红色告警。
- **收入自动存**：每笔收入按比例（默认 30%，可改）计入目标进度。
  注意这只是"记账意义上的存"——钱仍在你的总余额池里，只是目标进度前进。
- **储蓄率** =（本月收入 − 本月支出）÷ 本月收入，月总结必报。
- **冷静期**：新愿望清单项默认 7 天冷静期；到期弹「还想要吗？」
  回答放弃 → 记录「本次忍住了，省下 ¥X」，写入当月"省下的钱"统计。
- **多目标资金分配（已确认 ①A）**：收入自动存的 X% 全部计入优先级第一的目标；
  目标页可手动调拨已存金额。
- **买下目标时（已确认 ②A）**：自动记一笔对应支出（分类=购物）+ 目标标记"已达成"留档，
  账本余额与现实一致，饼图里能看到这笔大额消费。
- **放弃/暂停目标**：该目标已存金额释放回"未分配"（钱本身一直在总余额池里，不影响真实余额）。

### 11.5 总结设定

- 触发：看板手动按钮「生成周总结/月总结」；打开页面时若该写且未写，轻提示。
- 长度：周 200–400 字；月 400–800 字。
- 语气档位：朋友（默认）/ 毒舌 / 温柔 / 老师，设置里可换。
- 配图：开关，默认关（省钱）；打开则每周用生图 API 配一张小插画。
- 反模板化：每次生成提示词里要求换开头、换结构、必须引用本周具体事件。

### 11.6 愿望清单字段

名称 · 价格 · 图片/链接 · 优先级（拖拽排序）· 期望日期 · 状态
（想买 → 冷静期 → 进行中 → 已达成 / 已放弃）· 备注

### 11.7 数据库表（简）

- `transactions`：日期、金额、类型（支出/收入/退款/转账/取现/还款）、分类、商家、备注、来源（文字/截图/小票/CSV）、是否估算
- `line_items`：小票单品明细（品名、数量、单价），挂在交易下，供分类拆分与后续下钻
- `goals`：名称、价格、已存、优先级、状态、冷静期截止、创建时间
- `settings`：月预算、收入自动存比例、语气档位、起始余额、配图开关
- `summaries`：周期（周/月）、正文、生成时间
- `adjustments`：对账差额调整记录

### 11.8 小票拍照识别（新增输入类型）

小票和支付截图不同：小票上有**单品明细**，能做得更细。

- **适用图片**：购物小票、外卖/支付截图、淘宝京东订单截图、电子小票，一次可拖多张
- **AI 提取内容**：商家、收银时间（→ 交易日期）、实付总额（折后）、支付方式、
  单品明细（品名 × 数量 × 单价）。小票上"蒙牛纯牛奶250ml×1"这类简称由 AI 还原为常用名
- **跨分类拆分**：超市小票常常一张票多个分类——"牛奶→餐饮、纸巾→生活、笔→学习"，
  总额按单品自动拆到各分类，统计占比才真实
- **确认面板（关键设计）**：AI 提取结果先预览——每条记录可改日期/分类/金额、可删，
  点「确认入账」才写入。OCR 出错时不会污染账本，这是可靠性的底线
- **模糊小票兜底**：识别不出来时，图片保留，用户手填总额和分类

### 11.9 其余待定细节（我的建议附后，你表态）

1. **不计入统计的特殊类型**：取现、给家人转账、还朋友钱——这些不是"花销"，
   不该占预算、不该进饼图。建议单设「转账/取现/还款」类型，记账时标注，统计自动排除。
2. **AA / 代付**：帮室友带饭、聚餐 AA 是学生高频场景。建议 v1 简化约定：
   代付记支出+备注「XX 还」；收回时记收入（分类=还款）自动配对抵消；不做应收面板（v2 再说）。
3. **预算周期**：月预算每月 1 号重置，周总结从周一起算。够用就不做自定义。
4. **自动备份**：每次启动自动备份 SQLite 到 backups/ 文件夹，保留最近 30 份；
   设置里可一键导出 CSV。数据安全对学生项目很重要。
5. **目标达成**：达成后总结里恭喜，并报告「从立下目标到达成用了 X 天，
   期间忍住了 N 次冲动消费」；已达成目标留档可回看。
6. **API key 配置**：设置页填写，存本地配置文件，绝不上传别处。
7. **金额格式**：统一人民币元，支持「15」「15块」「¥15」「15.5」。

### 11.10 健壮性与杂项（设计底线）

1. **AI 挂了要明说（已确认）**：断网/API 失效时，看板顶部亮红色横幅
   「AI 不可用：请检查网络或 API 配置」，由用户自行解决，恢复后无需重启即可继续 AI 记账；
   期间输入区自动降级为手动表单（金额+分类+商家+日期）兜底，记账功能永不中断；
   总结生成失败可重试。
2. **解析失败不丢数据**：识别不出的原文/图片进入「待处理」队列，之后可手动处理。
3. **图片本地归档**：截图/小票原件存 data/images/ 按日期归档，可回看原始凭证。
4. **修改后总结会过期**：历史明细改/删后，已生成的周/月总结标记「已过期」，可一键重新生成。
5. **历史翻看**：看板默认本月，顶部月份切换器看任意历史月份的图表与总结。
6. **单机安全**：只监听 127.0.0.1，无需登录；不要把端口开放到公网。
7. **迁移备份**：整个项目文件夹拷走即可迁移，数据即文件。
