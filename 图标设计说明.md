# 🎨 启动器图标设计说明（Better-money）

设计基调：**简约 / 扁平 / 浅色系**，与应用的 🐷 小猪攒钱主题一致。

---

## 方案 A（备选，留档）：薄荷存钱罐

### 整体构图（画布 1024×1024）

```
┌────────────────────────────────────┐
│  ◜◝            ◜◝   ← 耳朵：两个圆角小三角（主体同色）
│ ╭──────────────────╮               │
│ │ ▐▔▔▔▔▔▔▔▔▔▔▔▔▔▌ │ ← 投币口：顶部圆角横槽（深绿）
│ │ ▐▌ ●        ●    │ ← 眼睛：两个小圆点（深绿）
│ │ ▐▌   ╭───╮       │ ← 鼻孔：两个更小的圆点
│ │ ▐▌   │ ¥ │       │ ← 硬币：金色圆，与身体右侧重叠
│ │ ▐▌   ╰───╯       │               │
│ ╰──────────────────╯               │
│         ╭╮                        │
│        ╰─────────╯ ← 底部椭圆浅影  │
└────────────────────────────────────┘
```

- **背景**：圆角方形（圆角半径 ≈ 画布 22%，即 228px 左右），浅薄荷白 `#EDF9F3`
  - 可选极淡线性渐变：左上 `#F5FBF8` → 右下 `#E3F4EC`（几乎看不出，只是让底子"透气"）
- **存钱罐身体**：胶囊形（圆角矩形，圆角 = 高度的一半）
  - 尺寸参考：宽约 480px、高约 380px，水平居中、垂直偏上（中心点在画布 46% 高度处）
  - 颜色：薄荷绿 `#35CE97`
- **投币口**：身体顶部一个圆角小横槽（约 150×34px），深绿 `#0B3D2E`
- **眼睛**：两个直径约 20px 的圆点，深绿 `#0B3D2E`，间距约 130px，位于投币口下方
- **鼻孔**：两个直径约 13px 的小圆点，同深绿色，在眼睛下方中央
- **耳朵**：两个圆角小三角（尖端圆润），与身体同色 `#35CE97`，从身体上缘伸出
- **硬币**：金色圆（直径约 120px）`#FFD45C`，外圈细描边 `#E8A93D`，
  中间一个粗体无衬线 `¥` 符号（`#8A5A00`），压在身体右下角、一半在身体外
- **高光**：身体左上一条白色短弧或小圆角块，`#FFFFFF` 60% 不透明度——这是"浅色系、通透感"的关键
- **阴影**：身体正下方一个椭圆，浅绿灰 `#CBE8DC`（约 40% 不透明度），高约 36px，不带投影方向

### 配色规范（浅色系）

| 用途 | 色值 | 说明 |
|---|---|---|
| 背景 | `#EDF9F3` | 薄荷白 |
| 主体 | `#35CE97` | 主薄荷绿 |
| 细节 | `#0B3D2E` | 深绿（投币口/眼/鼻） |
| 硬币 | `#FFD45C` | 浅金 |
| 硬币描边/¥ | `#E8A93D` / `#8A5A00` | 深金两档 |
| 高光 | `#FFFFFF` @60% | 通透感 |
| 阴影 | `#CBE8DC` | 浅绿灰 |

风格纪律：**扁平色块、不要外描边、不要投影（只有底部椭圆影）、不要文字、元素越少越好**。

### 小尺寸可读性测试

把图缩到 32px、16px 看：应仍能认出"圆身体 + 投币口 + 金币"三个要素。
如果发糊，按顺序删元素：腮红（如果有）→ 眼睛/鼻孔 → 高光 → 阴影，只留"身体+槽+币"。

---

## 方案 B（备选）：新芽硬币

寓意"攒钱会生长"，构图更简单，小尺寸表现最好。

- 背景：同方案 A 的浅薄荷圆角方形
- 主体：一枚浅金硬币（`#FFD45C`，直径约 420px，居中）
- 硬币上方/中央：两片薄荷绿嫩芽（`#35CE97`，两条弧线叶形，像小苗从硬币里长出来）
- 细节：硬币内一圈浅描边 + 一个小 `¥`（可选，嫌挤可去掉）
- 高光/阴影：同 A 的处理

## ✅ 已选定主方案：向上钱包（方案 C 详细规格）

寓意"余额向上"。元素只有三个：**浅绿圆角卡片 + 白色 ¥ + 浅金上升箭头**。

### 构图（画布 1024×1024）

```
┌────────────────────────────────────┐
│                          ▲         │
│                        ╱ ╲        │
│                      ╱   ╲   ← 箭头头部（浅金，尖端在右上）│
│      ╱               ╰────╯      │
│     ╱   ╭──────────────╮         │
│    ╱    │              │         │
│   ╱     │      ¥       │  ← 卡片（薄荷绿圆角矩形）│
│  ╱      │              │         │
│ ╱       ╰──────────────╯         │
│╱                                  │
│ ← 箭头中段藏在卡片后面（左下进入）   │
└────────────────────────────────────┘
```

**层级关系（关键）**：箭头从画面左下角进入，**中段被卡片挡住**，从卡片右上角上方探出箭头头部。
这样只看到"左下一条金线 + 右上金色箭头"，既简洁又有"从卡片里长出来"的故事感。

### 元素规格

**① 背景（圆角方形）**
- 圆角半径 ≈ 画布 22%（约 228px）
- 底色 `#EDF9F3` 浅薄荷白；极淡线性渐变：左上 `#F5FBF8` → 右下 `#E3F4EC`

**② 卡片（钱包）**
- 横版圆角矩形：宽约 580px、高约 400px，圆角约 96px，**水平居中、不倾斜**
- 填充 `#35CE97` 薄荷绿（可选极淡纵向渐变 `#3DD4A3` → `#2FC08D`）
- 顶部内侧一条白色高光条：宽同卡片、高约 46px、圆角对齐、`#FFFFFF` 45% 不透明度，
  压在卡片上缘内侧 10px 处——"浅色通透感"的来源
- 无外描边、无投影（投影只给底部椭圆影）

**③ ¥ 符号**
- 白色 `#FFFFFF`，粗体无衬线（思源黑体 / Arial Rounded 一类，字重 700+）
- 字高约 170px，居卡片正中心
- 可选：符号下方加一层 `#1FAF7C` 10% 的柔影，帮助白色压住薄荷绿

**④ 上升箭头**
- 浅金 `#F5C04E`
- 箭杆：圆头描边线，宽约 60px，从画面左下 (170, 780) 斜向上约 38°，
  穿过卡片中段（被卡片遮住），至 (830, 260) 收尾
- 箭头头部：等腰三角，长约 150px、张角约 70°，尖端在 (830, 260)，转角做 8px 圆角
- 全路径无描边、无阴影（扁平）

**⑤ 底部椭圆影**
- 卡片正下方椭圆：宽约 480px、高约 36px，`#CBE8DC`（40% 不透明度），无方向性

### 配色规范（浅色系）

| 用途 | 色值 | 说明 |
|---|---|---|
| 背景 | `#EDF9F3` | 薄荷白 |
| 卡片 | `#35CE97` | 主薄荷绿 |
| ¥ | `#FFFFFF` | 纯白 |
| 箭头 | `#F5C04E` | 浅金（比存钱罐方案的 `#FFD45C` 稍柔） |
| 高光 | `#FFFFFF` @45% | 卡片顶部光带 |
| 阴影 | `#CBE8DC` | 底部椭圆 |

### 可选微调（做了更好看，不做也成立）

- 箭头头部旁加 1-2 个金色小圆点/四角星（直径 24px，`#F5C04E` 60%）——"增长感"点缀
- 卡片右下角放一个极小的白色圆点装饰（与 ¥ 呼应）

### 小尺寸可读性测试

缩到 32px、16px 检查，必须仍能认出三个要素：**卡片块 + 白 ¥ + 金色斜线箭头**。
如果发糊，按顺序处理：去掉渐变 → 箭头加粗到 72px → ¥ 相对放大 → 去掉底部椭圆影。

### 变体（做主打版就行，喜欢可以各来一张）

- **变体 A（箭在卡内）**：箭头从卡片左下角内侧画到右上角内侧，¥ 移偏左下——全部元素收在卡片里
- **变体 B（箭出卡外）**：卡片不变，箭头从卡片右缘中部向画面右上"飞出"，左下角不放金线

### 交付规格

| 项目 | 要求 |
|---|---|
| 画布 | 1024×1024 |
| 导出 | 圆角背景版 PNG + 透明背景版 PNG（去掉圆角底色，图形居中；卡片加 3px、`#FFFFFF` 25% 内描边，保证深色桌面上轮廓清晰） |
| 尺寸组 | 512 / 256 / 128 / 64 / 48 / 32 / 16 |
| 转 .ico | `magick icon.png -define icon:auto-resize=16,24,32,48,64,128,256 icon.ico` 或在线工具 |

### 套用

1. `icon.ico` 放进项目文件夹
2. 右键 `启动.bat` → 发送到 → 桌面快捷方式 → 右键快捷方式 → 属性 → 更改图标
3. 同时发我一份，我接成网页 favicon 并推 GitHub
