# 🎨 启动器图标设计说明（Better-money）

设计基调：**简约 / 扁平 / 浅色系**，与应用的 🐷 小猪攒钱主题一致。

---

## 方案 A（主推）：薄荷存钱罐

### 整体构图（画布 1024×1024）

```
┌────────────────────────────────────┐
│  ◜◝            ◜◝   ← 耳朵：两个圆角小三角（主体同色）
│ ╭──────────────────╮               │
│ │ ▐▔▔▔▔▔▔▔▔▔▔▔▔▔▌ │ ← 投币口：顶部圆角横槽（深绿）
│ │ ▐▌ ●        ●    │ ← 眼睛：两个小圆点（深绿）
│ │ ▐▌   ╭───╮       │ ← 鼻孔：两个更小的圆点
│ │ ▐▌   │ ¥ │       │ ← 硬币：金色圆，与身体右侧重叠
│ │ ▐▌   ╰───╯       │               │
│ ╰──────────────────╯               │
│         ╭╮                        │
│        ╰─────────╯ ← 底部椭圆浅影  │
└────────────────────────────────────┘
```

- **背景**：圆角方形（圆角半径 ≈ 画布 22%，即 228px 左右），浅薄荷白 `#EDF9F3`
  - 可选极淡线性渐变：左上 `#F5FBF8` → 右下 `#E3F4EC`（几乎看不出，只是让底子"透气"）
- **存钱罐身体**：胶囊形（圆角矩形，圆角 = 高度的一半）
  - 尺寸参考：宽约 480px、高约 380px，水平居中、垂直偏上（中心点在画布 46% 高度处）
  - 颜色：薄荷绿 `#35CE97`
- **投币口**：身体顶部一个圆角小横槽（约 150×34px），深绿 `#0B3D2E`
- **眼睛**：两个直径约 20px 的圆点，深绿 `#0B3D2E`，间距约 130px，位于投币口下方
- **鼻孔**：两个直径约 13px 的小圆点，同深绿色，在眼睛下方中央
- **耳朵**：两个圆角小三角（尖端圆润），与身体同色 `#35CE97`，从身体上缘伸出
- **硬币**：金色圆（直径约 120px）`#FFD45C`，外圈细描边 `#E8A93D`，
  中间一个粗体无衬线 `¥` 符号（`#8A5A00`），压在身体右下角、一半在身体外
- **高光**：身体左上一条白色短弧或小圆角块，`#FFFFFF` 60% 不透明度——这是"浅色系、通透感"的关键
- **阴影**：身体正下方一个椭圆，浅绿灰 `#CBE8DC`（约 40% 不透明度），高约 36px，不带投影方向

### 配色规范（浅色系）

| 用途 | 色值 | 说明 |
|---|---|---|
| 背景 | `#EDF9F3` | 薄荷白 |
| 主体 | `#35CE97` | 主薄荷绿 |
| 细节 | `#0B3D2E` | 深绿（投币口/眼/鼻） |
| 硬币 | `#FFD45C` | 浅金 |
| 硬币描边/¥ | `#E8A93D` / `#8A5A00` | 深金两档 |
| 高光 | `#FFFFFF` @60% | 通透感 |
| 阴影 | `#CBE8DC` | 浅绿灰 |

风格纪律：**扁平色块、不要外描边、不要投影（只有底部椭圆影）、不要文字、元素越少越好**。

### 小尺寸可读性测试

把图缩到 32px、16px 看：应仍能认出"圆身体 + 投币口 + 金币"三个要素。
如果发糊，按顺序删元素：腮红（如果有）→ 眼睛/鼻孔 → 高光 → 阴影，只留"身体+槽+币"。

---

## 方案 B（备选）：新芽硬币

寓意"攒钱会生长"，构图更简单，小尺寸表现最好。

- 背景：同方案 A 的浅薄荷圆角方形
- 主体：一枚浅金硬币（`#FFD45C`，直径约 420px，居中）
- 硬币上方/中央：两片薄荷绿嫩芽（`#35CE97`，两条弧线叶形，像小苗从硬币里长出来）
- 细节：硬币内一圈浅描边 + 一个小 `¥`（可选，嫌挤可去掉）
- 高光/阴影：同 A 的处理

## 方案 C（备选）：向上钱包

- 背景：浅色圆角方形（可选浅奶油 `#FFF9EC` 与金币更搭）
- 主体：极简钱包/卡片形（圆角矩形），`#35CE97`
- 卡片中央：白色 `¥`，卡片右上一根浅金上升箭头（折线）
- 寓意：余额向上

---

## 制作与交付规格

| 项目 | 要求 |
|---|---|
| 画布 | 1024×1024（首选） |
| 导出 | 圆角背景版 PNG ×1 + 透明背景版 PNG ×1（图形居中，去掉圆角底色） |
| 尺寸组 | 512 / 256 / 128 / 64 / 48 / 32 / 16（Windows 图标需要） |
| 格式 | PNG（透明版保留 alpha 通道） |
| 转 .ico | 在线工具，或 ImageMagick：`magick icon.png -define icon:auto-resize=16,24,32,48,64,128,256 icon.ico` |

### 套用到启动器

1. 把 `icon.ico` 放进项目文件夹
2. 右键 `启动.bat` → 发送到 → 桌面快捷方式
3. 右键桌面快捷方式 → 属性 → 快捷方式 → 更改图标 → 浏览选 `icon.ico`
4. （可选）快捷方式重命名为「🐷 记账」

### 顺带利用

图标做好后还可以当**网页 favicon**（浏览器标签页小图标，当前还是 404），
告诉我一声我就接进 `static/`。
