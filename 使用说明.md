# 🐷 Better-money 使用说明

> 一个跑在你电脑上的个人记账与攒钱助手：每晚花几分钟把花销"说"给它，
> 它帮你算余额、看图表、攒目标、每周每月写一篇像朋友一样的小作文总结。

---

## 目录

1. [这是什么](#1-这是什么)
2. [安装与启动](#2-安装与启动)
3. [首次配置（5 分钟）](#3-首次配置5-分钟)
4. [每日记账指南](#4-每日记账指南)
5. [看板与图表](#5-看板与图表)
6. [目标清单（愿望清单）玩法](#6-目标清单愿望清单玩法)
7. [周/月总结](#7-周月总结)
8. [历史明细与修改](#8-历史明细与修改)
9. [对账校准](#9-对账校准)
10. [数据、备份与迁移](#10-数据备份与迁移)
11. [AI 供应商配置](#11-ai-供应商配置)
12. [常见问题（FAQ）](#12-常见问题faq)
13. [安全与隐私](#13-安全与隐私)
14. [成本参考](#14-成本参考)
15. [测试与二次开发](#15-测试与二次开发)

---

## 1. 这是什么

Better-money 是一个**纯本地**运行的个人记账 Web 应用，专为"想攒钱但攒不住钱"的场景设计：

- **记账省事**：文字一口气写多笔、发小票截图、导入微信/支付宝账单，全自动解析
- **随时有数**：当前余额、本月支出、今天还能花多少，打开就能看到
- **目标驱动**：想买的东西进"愿望清单"，冷静期拦截冲动消费，收入自动存进目标
- **人情味总结**：每周/每月生成一篇引用真实事件的小作文（不是固定模板）
- **图表直观**：分类占比、支出趋势、周对比、目标进度环

源码运行时，数据默认保存在项目的 `data/` 文件夹；未来 Windows 安装版会把数据放到
`%LOCALAPPDATA%\BetterMoney` 下。不需要联网也能记账（AI 解析功能需要配置 API）。

---

## 2. 安装与启动

### 2.1 需要什么

- **Windows**：双击 `启动.bat` 即可（Python 由启动脚本自动使用；首次运行需要联网安装依赖）
- **macOS**：需要 Python 3.10+（系统自带或 `xcode-select --install` / `brew install python` 安装）；首次运行需要联网
- 一个能打开浏览器的环境（Chrome / Edge / Safari 均可）

### 2.2 启动（Windows）

双击项目文件夹里的 **`启动.bat`**（或桌面上的 Better-money 快捷方式）：

1. 首次运行：自动创建运行环境并安装依赖（约 1-3 分钟，需要联网；失败会给出明确提示）
2. 启动器拉起后台服务，**等服务真正就绪、通过健康检查后**才打开浏览器
   ——不会再出现"第一次点了打不开、要点第二次"的情况
3. 再点一次启动脚本 = 打开同一个实例（不会重复启动、不抢端口）；
   即使 8642 端口被别的程序占用，也会自动换一个可用端口
4. 黑色命令行窗口一闪而过是正常的，**服务在后台运行**；
   停止：网页内「设置 → 退出服务」，或双击 `停止服务.bat`

**创建桌面/开始菜单快捷方式**（可选）：右键 `tools\make_shortcut.ps1` →
「使用 PowerShell 运行」，会在桌面和开始菜单生成带图标的 Better-money.lnk。
建议把快捷方式的起始位置保留在项目文件夹；**移动整个项目文件夹后快捷方式
需要重新创建**。

### 2.3 启动（macOS）

双击项目文件夹里的 **`启动.command`**：

1. 首次运行：自动创建虚拟环境并安装依赖（清华镜像加速）
2. 自动打开浏览器：**http://127.0.0.1:8642**
3. 终端窗口开着 = 服务在运行；**关掉窗口 = 停止服务**（或双击 `停止服务.command`）

**另一种方式：`Better-money.app`**（无窗口后台运行）

- 双击 `.app` → 后台静默启动 + 打开浏览器，**没有终端窗口**
- 停止：双击 `停止服务.command`
- 拖进 Dock：右键 `.app` →「制作替身」，把**替身**拖到 Dock（`.app` 本体留在项目文件夹里，别移走）
- 第一次打开若提示"身份不明的开发者"：**右键 → 打开**；或终端执行
  `xattr -dr com.apple.quarantine "项目文件夹路径"` 一次性解除

**macOS 排错速查**：

| 问题 | 处理 |
|---|---|
| 双击 `.command` 提示权限不足 | 终端执行 `chmod +x 启动.command 停止服务.command` |
| 提示"已损坏/无法打开" | Gatekeeper 拦截：右键 → 打开；或执行上面的 xattr 命令 |
| 提示找不到 Python | `xcode-select --install`，或装 Homebrew 后 `brew install python` |
| 依赖安装慢/失败 | 脚本已默认清华镜像；仍失败请检查网络 |
| 换电脑（Intel ↔ Apple 芯片） | 删除 `.venv` 文件夹后重新双击启动（venv 不跨架构） |

### 2.4 手动启动（可选）

```bat
.venv\Scripts\activate
python -m uvicorn app.main:app --host 127.0.0.1 --port 8642
```

> 提示：服务只监听本机 127.0.0.1，**不要**把端口开放到公网。
> 启动脚本会自动选择可用端口（默认 8642，被占用时自动换一个）。

---

## 3. 首次配置（5 分钟）

首次打开网页会弹出**四步引导**（完成前无法关闭）：

1. **起点**：全新开始 / 迁移旧数据（选旧数据文件夹，显示笔数后确认导入）/
   从备份恢复（选 `.zip` 完整备份）
2. **初始余额**：起始日期 + 初始余额（起始日之前微信 + 支付宝 + 银行卡 + 现金的加总）
3. **预算**：月预算 + 收入自动存比例（0~1）
4. **AI（可选）**：选服务商（OpenAI / DeepSeek / Qwen / 自定义）、填 API Base、
   API Key、模型名，点「测试连接」验证；不想用 AI 直接「跳过 AI 完成」

之后可在 **「设置」页**随时修改预算、比例、语气和 AI 配置。余额公式为：

```
当前余额 = 初始余额 + 收入 + 退款 − 支出 − 取现/转账/还款 + 对账差额
```

> 「取现」「转账」「还款」不会算进花销统计，也不占预算、不进饼图——
> 它们只是钱挪了个位置。
>
> 初始余额和起始日期是**一次性基准**：之后月份会自动滚动结算，不需要每月
> 再填。要改只能点设置页「更正初始余额」（变更前自动创建安全备份）。

---

## 4. 每日记账指南

看板页左侧是记账区，**从上到下**依次是：日期选择、智能记账框、截图/小票上传、账单导入、手动表单。

### 4.1 🤖 智能文字记账（主力，推荐）

把当天花销一口气写进大输入框，点 **「🤖 智能解析入账」**：

```
午饭食堂 15
奶茶 12
打印资料 5
```

一次解析多笔，自动入账并刷新看板。

**支持的写法速查**：

| 你想表达 | 写法示例 | 效果 |
|---|---|---|
| 普通一笔 | `午饭食堂 15` | 支出 15，分类=餐饮 |
| 一行多笔 | `食堂15和奶茶12` | 拆成两笔 |
| 收入 | `兼职200` / `收到红包66` / `家里给了500` | 识别为收入并按比例自动存入第一目标 |
| 退款 | `退了奶茶12` | 冲减奶茶咖啡分类的支出 |
| AA 分摊 | `聚餐 200 4人AA` | 记 50，备注保留"原价200" |
| AA 实付 | `聚餐 AA后我付了50` | 直接记 50 |
| 请客 | `请室友吃饭 120`（不写 AA） | 记全额 |
| 补记昨天 | `昨天午饭 15` | 自动记到昨天 |
| 估算金额 | `超市大概30` | 记 30 并标记"≈估算"，可后改 |
| 取现/转账/还款 | `取现200` / `给家里转账500` / `还张三50` | 类型分别为取现/转账/还款，不计入花销统计 |

**细节约定**：

- 金额写不写单位都行：`15`、`15块`、`¥15`、`15.5` 都认识
- 完全没有金额的条目（如"买了瓶水"）不会瞎猜，会反问「多少钱？」，
  补答后再记
- 相同"日期+金额+商家"的重复提交会自动跳过并提示，防止记两遍
- 记账后看板余额、今日可花、图表**立刻刷新**；记错了到
  [历史明细](#8-历史明细与修改) 改或删

### 4.2 ✍️ 手动记账（兜底，永远可用）

智能框下方"或手动记一笔"：选类型（支出/收入/退款/取现/转账/还款）、填金额、
选分类、商家、备注 → 「✔ 记一笔」。AI 挂掉、断网时靠它，功能不受影响。

### 4.3 📷 上传截图/小票

- 点 **「📷 上传截图/小票」** 选择图片（支持多张，`.jpg/.png/.webp`，单张不超过 10 MiB，一次最多 10 张）
- 图片上方有**说明框**：AA 场景填 `4人AA` 或 `AA后我付了50`，请客场景填 `我请客`
- AI 识别：商家、收银时间（作为交易日期）、**折后实付总额**、单品明细
- 超市小票会**自动跨分类拆分**：牛奶→餐饮、纸巾→生活、笔→学习
- 识别结果进**确认面板**：每条可改日期/类型/分类/商家/金额/备注、可删，
  点「✔ 确认入账」才写入账本——OCR 看错了也不会污染账
- 小票太糊识别不出 → 图片保留在本地，手填即可

### 4.4 📄 导入账单 CSV/Excel（月底补录神器）

几天没记账？微信/支付宝都支持导出账单，一次补齐：

1. **微信**：我 → 服务 → 钱包 → 账单 → 常见问题 → 下载账单（选时间范围）
2. **支付宝**：我的 → 账单 → 右上角 → 开具交易流水证明 / 下载账单
3. 导出 `.csv` 或 `.xlsx` 文件后，点 **「📄 导入账单 CSV」** 选择文件

自动处理规则：

| 账单里的项目 | 记成 |
|---|---|
| 商户消费 | 支出（分类=其他，可在确认面板改） |
| 红包收入 | 收入 / 红包 |
| 转账 | 转账（不计入花销） |
| 零钱提现 | 取现（不计入花销） |
| 支付宝交易分类（餐饮美食/交通出行…） | 自动映射到你的 8 类体系 |
| 收/支为"/"的行（不计收支） | 跳过 |

导入结果同样走**确认面板**，重复导入自动去重（日期+金额+商家相同跳过）。

---

## 5. 看板与图表

看板是首页，也是你每天唯一的"仪表盘"：

- **顶部五张卡**：当前余额 / 本月支出 / 本月收入 / 月预算 / 今日可花额度
  - 今日可花 =（月预算 − 本月已花）÷ 本月剩余天数，每天刷新，超支变红
- **预算预警横幅**：本月支出达预算 80% 黄色提醒"剩余天数日均需控制在 ¥X"；
  超 100% 红色告警
- **四张图**：分类占比（环形，退款自动冲减）、近 30 天支出趋势（折线）、
  近 8 周对比（柱状）、目标进度列表
- **目标进度列表**：所有「冷静期 / 进行中 / 已暂停」目标都会显示，各带
  「已存 / 需要 / 还差」和百分比；第一个活跃目标标「当前优先目标」，
  目标多了列表会滚动而不是截断
- **图表月份切换器**：翻看任意有记录的历史月份（分类占比与趋势跟随切换；
  顶部卡片始终是"本月"口径）
- **最近记录**：最新的 50 条流水，可快速删除

---

## 6. 目标清单（愿望清单）玩法

顶部「目标清单」页签。想买什么先挂在这里，而不是直接下单：

### 6.1 加入清单

填名称（如"降噪耳机"）、价格，可选期望日期和备注/链接 → 「➕ 加入愿望清单」。
新目标自动进入**冷静期**（默认 7 天，可在配置里改）。

### 6.2 冷静期到期

卡片上会出现两个按钮：

- **😍 还想要** → 转为"进行中"，开始攒
- **🧘 不要了** → 转为"已放弃"，并把价格计入「🏆 本月省下的钱」
  （页面顶部横幅会统计：忍住了 N 次，共省下 ¥X）

### 6.3 攒钱规则

- **收入自动存**：每笔收入按设置的比例（默认 30%）自动存入目标——按优先级
  **从上到下依次填满**（每个目标只填到价格为止），文字、手动、CSV 导入的收入都触发
- **优先级**：卡片上的 ↑↓ 调整顺序；自动存永远先给排最前的
- **调拨**：编辑目标时可以改"已存金额"，实现目标间资金转移
- **暂停/恢复**：暂停 = 冻结已存金额；恢复继续
- **达成**：点"达成"弹窗二选一——
  - **我买了**：自动记一笔支出（分类=购物，金额=价格），账本和现实一致
  - **先不买**：冻结该目标，钱留着，继续攒下一个
- **放弃**：已存金额清零（钱本身一直在你的总余额池里，不受影响）
- **删除**：目标卡片的「删除」会先确认；删除只移除目标与规划金额，
  **不会改变账本余额**，已存的金额回到可用余额池
- **看板进度**：看板的目标进度列表显示全部活跃目标（见 [第 5 节](#5-看板与图表)）

---

## 7. 周/月总结

顶部「总结」页签：

- **✍️ 生成总结**：一个按钮打开面板，先选类型（周总结 200-400 字 / 月总结
  400-800 字），再选区间——**本周 / 上周 / 本月 / 上月**快捷按钮，或
  **自定义任意起止日期**（最长 366 天）。周/月类型和区间可以自由组合
  （比如给任意 10 天写一篇"周总结"）
- 同一类型+区间已存在总结时，会提示**覆盖重写**（确认后重新生成，不新增记录）

总结不是套模板：AI 会拿到你的真实流水、分类支出、常去商家、每日支出、大额单、
收入、上周对比、预算执行、目标进度、冷静期战绩，然后写一篇**引用具体事件**
（如"周二食堂那笔 15 块"）的小作文——先肯定做得好的，再点 1-2 个问题，
结尾给一条下周能照着做的小建议。每次生成会避开上次的开头句式。

- **语气档位**：朋友（默认）/ 毒舌 / 温柔 / 老师，设置页随时换
- **重新生成**：总结卡片上的「重新生成」会带出原类型与区间，覆盖重写
- **删除**：总结卡片上的「删除」确认后删除该篇总结；**账目、目标和余额完全不受影响**
- **过期提示**：之后改账/删账，覆盖该日期的总结会标黄"账目已修改，可能过期"，
  重新生成即可
- **配图**：默认关（省钱）。想开启在 `data/config.json` 里设
  `"image_gen_enabled": true`，每次总结附一张插画；生图失败不影响正文
- **轻提示**：有记账但本周没写总结时，总结页顶部会提醒你

---

## 8. 历史明细与修改

顶部「历史明细」页签：

- **月份切换**：看任意月份的完整流水
- **编辑**：改日期/金额/类型/分类/商家/备注（会标记相关总结过期）
- **删除**：删单笔
- **≈估算标记**：AI 按"大概30"记的金额带此标签，方便日后核对
- **来源列**：手动 / 文字 / 小票 / CSV / 目标，知道每笔怎么来的
- **待处理队列**：AI 挂掉或解析失败时，你的原文/图片会保留在这里
  （页面顶部黄色区块），可随时删除，数据不丢

---

## 9. 对账校准

设置页 →「🔍 对账校准」：

卡里钱和账本对不上时（现金花了忘记、漏了几笔），输入**真实余额**，
差额自动记为"调整"入账，账本余额即刻修正。建议每周校准一次。

下方「调整历史」列出所有校准记录，未撤销的可以点**「撤销」**：撤销会记一笔
相反的调整，余额回到校准之前，原记录保留可查。

---

## 10. 数据、备份与迁移

数据位置取决于启动方式：

- **源码运行**：默认数据根目录是项目文件夹，实际数据在项目的 `data/`
- **未来 Windows 安装版**：应用数据根目录将是 `%LOCALAPPDATA%\BetterMoney`；
  其中账本和配置在 `data\`，备份在 `backups\`。目前这描述的是安装版目标，不表示安装包已经发布
- **开发测试**：把 `BETTER_MONEY_HOME` 设为 `%TEMP%` 下的一次性目录；
  测试数据在 `$env:BETTER_MONEY_HOME\data`，绝不能把变量指向项目根目录

源码模式的目录如下：

```
data/
├── better_money.db   # SQLite 数据库（账本、目标、总结）
├── config.json       # 你的设置和 API Key
├── images/           # 上传的小票/截图原件、总结配图
└── backups/          # 经校验的 ZIP 备份
```

- **自动备份**：每次启动服务时创建经校验的 ZIP 归档，源码模式保存在
  `data/backups/`，保留最近 **30** 份自动备份
- **备份控件**：设置页「数据备份」区块——
  - 「立即备份」：马上创建一份含图片的完整 ZIP
  - 「恢复备份」：选择 `.zip` 完整备份恢复（恢复前自动先做一次安全备份，
    恢复会校验清单、数据库和归档内容）
  - 「导出完整备份 ZIP」：下载最近一份完整备份
  - 「打开数据文件夹」：直接打开数据目录
  - 「导出全部交易 CSV」（带 BOM，Excel 直接打开不乱码）
- **ZIP 内容**：数据库、已去除 API Key 的配置、`manifest.json` 清单；
  创建时选择包含图片，才会加入 `data/images/` 下的文件
- **换电脑/迁移**：源码模式仍可整体拷走项目文件夹；跨安装方式迁移优先使用完整 ZIP；
  旧版本数据可用首次引导里的「迁移旧数据」导入

---

## 11. AI 供应商配置

AI 层是**可切换适配器**：所有供应商都走 OpenAI 兼容接口，改设置页的几个字段
（或 config.json）即可切换，无需改代码。

### 11.1 常见组合

设置页「AI 服务商」下拉可直接选预设（自动填好 API Base），也可选「自定义」自己填：

| 方案 | 文本解析/总结 | 截图/小票识别 | api_base 示例 |
|---|---|---|---|
| OpenAI | gpt-4o-mini | gpt-4o | https://api.openai.com/v1 |
| DeepSeek（国内直连） | deepseek-chat | 需另配视觉模型 | https://api.deepseek.com |
| 阿里 Qwen（国内直连） | qwen-plus | qwen-vl-plus | https://dashscope.aliyuncs.com/compatible-mode/v1 |

填好后点 **「测试 AI 连接」**：用一条"回复 OK"的单 token 请求验证网络、Key 和
模型名，成功显示「连接成功 ✓」，失败显示具体原因（Key 不会写进日志）。

> 设置页只有 **一个** api_base / api_key，所以"DeepSeek 文本 + Qwen 视觉"这种
> 跨家组合暂不能同时配（可后续扩展）；单家用哪家填哪家即可。

### 11.2 进阶设置（编辑 `data/config.json`）

设置页没暴露的字段，停止服务后用记事本编辑 `data/config.json`：

| 字段 | 默认值 | 说明 |
|---|---|---|
| `model_text` | gpt-4o-mini | 文字解析与总结用的模型 |
| `model_vision` | gpt-4o | 图片识别用的模型 |
| `model_image` | gpt-image-1 | 总结配图用的模型 |
| `image_gen_enabled` | false | 总结配图开关 |
| `cooldown_days` | 7 | 愿望清单冷静期天数 |

### 11.3 AI 挂了怎么办

断网、Key 失效、额度用完时：

- 顶部出现**红色横幅**「AI 不可用：请检查网络或 API 配置」
- 期间**手动记账照常可用**，功能不中断
- 你发的内容会进"待处理队列"，不丢
- 解决后**无需重启**，直接继续用 AI 记账

---

## 12. 常见问题（FAQ）

**Q：打不开网页？**
先双击 `停止服务.bat` 再重新双击 `启动.bat`；仍不行看命令行提示与
`logs\startup.log`。启动器只会在服务健康检查通过后才打开浏览器。

**Q：AI 解析结果不对？**
到「历史明细」编辑该笔；或把要记的内容写得明确些（金额+分类词）。

**Q：账本余额和实际对不上？**
设置页「对账校准」输入真实余额即可，差额自动调整。

**Q：CSV 导入后金额重复了？**
导入本身有去重（日期+金额+商家）；如仍重复，确认面板里删掉再确认。

**Q：旧版 .xls 导入失败？**
不支持旧版 .xls，请从微信/支付宝导出 .csv 或 .xlsx。

**Q：总结和我的账对不上？**
改过账后总结会标"过期"，重新生成即可。

**Q：数据库损坏/误删？**
从备份目录选择最近一份有效的 `.zip` 完整备份恢复（见第 10 节）。

**Q：想换电脑？**
源码模式可整体拷走项目文件夹；跨安装方式迁移优先使用 `.zip` 完整备份。

---

## 13. 安全与隐私

- 服务只监听 `127.0.0.1`，仅供本机使用，无需登录
- API Key 只存在当前数据目录的 `data/config.json`，不会写进完整备份，也不上传任何别处
- **注意**：你提交给 AI 解析的文字、截图、账单内容会发送给你配置的大模型服务商
  （这是"AI 理解你的话"的必然过程）。个人自用风险可控，建议：
  - 导入账单前剔除卡号等敏感列
  - 不在备注里写密码、身份证号等信息
  - 很在意隐私的话，未来可把 AI 层换成本地部署的小模型

---

## 14. 成本参考

按学生体量（每月约 100 笔文字 + 30 张截图 + 4 篇周总结 + 1 篇月总结）估算：

| 项目 | 费用 |
|---|---|
| gpt-4o-mini 文本解析与总结 | 约 ¥1-3 / 月 |
| gpt-4o 截图识别 | 约 ¥2-5 / 月 |
| 图表 | ¥0（本地渲染） |
| 合计 | **每月 5 元以内** |

用 DeepSeek / Qwen 国内模型会更便宜（甚至每月几分钱量级），且不用翻墙。

---

## 15. 测试与二次开发

### 目录结构

```
Better-money/
├── app/                # 后端
│   ├── main.py         # FastAPI 入口与全部 API
│   ├── db.py           # SQLite 建表与访问
│   ├── ai.py           # AI 层（文字解析/图片识别，可切换供应商）
│   ├── summarizer.py   # 周/月总结生成（素材收集+提示词）
│   ├── importers.py    # 微信/支付宝账单解析
│   ├── backup.py       # 启动自动备份
│   └── config.py       # 配置管理
├── static/             # 前端（HTML/CSS/JS + ECharts 本地库）
├── tests/              # 测试：六套端到端 + 回归脚本 + mock LLM 服务器
├── 设计文档.md          # 完整设计决策记录
├── README.md
├── 使用说明.md          # 本文档
└── 启动.bat
```

### 运行回归测试

一条命令跑完整业务回归（真实服务 + M2~M7 端到端脚本 + pytest 单元/契约套件）。
`tests\run_all.py` 会拒绝缺失的 `BETTER_MONEY_HOME`，也会拒绝项目根目录，
测试数据只会写进 `%TEMP%` 下的一次性目录：

```powershell
$testRoot = Join-Path $env:TEMP ('better-money-tests-' + [guid]::NewGuid().ToString('N'))
$env:BETTER_MONEY_HOME = $testRoot
# 终端里先启动真实服务（回归脚本自带 mock LLM 的启动方式见 tests\test_real_legacy_runner.py）
.\.venv\Scripts\python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8642
# 另一个终端：
$env:BETTER_MONEY_TEST_BASE_URL = "http://127.0.0.1:8642"
.\.venv\Scripts\python.exe tests\run_all.py
```

只跑单元/契约套件（不需要真实服务）：

```powershell
.\.venv\Scripts\python.exe -m pytest tests\test_paths.py tests\test_migrations.py `
  tests\test_backup.py tests\test_legacy_migration.py tests\test_data_api.py `
  tests\test_startup_recovery.py tests\test_run_all_guard.py tests\test_goals.py `
  tests\test_goal_ui_contract.py tests\test_summaries_api.py tests\test_summary_ui_contract.py `
  tests\test_ledger.py tests\test_onboarding_ui_contract.py tests\test_ai_settings.py `
  tests\test_uploads.py tests\test_real_legacy_runner.py -p no:cacheprovider
```

### 常用 API 速查

`/api/summary` 看板汇总 · `/api/stats` 图表数据 · `/api/transactions` 流水
增删改查 · `/api/parse_text` 文字解析 · `/api/upload_images` / `/api/import_csv`
/ `/api/confirm_items` 识别确认 · `/api/goals` 目标管理 · `/api/summaries`
总结（区间生成 / 删除） · `/api/reconcile` + `/api/adjustments` 对账与撤销 ·
`/api/settings/initial-balance` 受保护的初始余额 · `/api/settings/test-ai`
AI 连接测试 · `/api/backups` 备份管理 · `/api/migration/*` 旧数据迁移 ·
`/api/export/*` 导出

---

祝攒钱顺利 🐷✨ 有问题随时回来找它（和我）。
